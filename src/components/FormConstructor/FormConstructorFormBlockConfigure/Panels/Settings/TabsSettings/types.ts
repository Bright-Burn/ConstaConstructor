import { TabsPropLinePosition, TabsPropSize, TabsPropView } from '@consta/uikit/Tabs'

export const linePositionArray: TabsPropLinePosition[] = ['bottom', 'top', 'left', 'right']

export const viewArray: TabsPropView[] = ['bordered', 'clear']

export const sizeArray: TabsPropSize[] = ['xs', 's', 'm']

export const fitModeArray: FitMode[] = ['dropdown', 'scroll']
export type FitMode = 'dropdown' | 'scroll'
